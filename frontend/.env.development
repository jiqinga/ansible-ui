# 🌐 前端开发环境配置

# 后端API地址
# - 留空或注释掉：使用 Vite 代理（开发环境推荐）
# - 设置完整URL：直接请求后端（如：http://127.0.0.1:8000）
# 注意：不要包含 /api/v1 路径，这个前缀会在 apiClient.ts 中自动添加
# VITE_API_BASE_URL=

# 后端端口（用于 Vite 代理配置）
VITE_BACKEND_PORT=8000
